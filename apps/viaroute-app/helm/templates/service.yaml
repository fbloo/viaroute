apiVersion: v1
kind: Service
metadata:
  name: {{ include "viaroute-app.fullname" . }}
  labels:
    {{- include "viaroute-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "viaroute-app.selectorLabels" . | nindent 4 }}

